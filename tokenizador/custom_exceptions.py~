from exceptions import Exception

class InvalidToken(BaseException):
    def __init__(self, token):
        self.message = 'Token %s is invalid' % token

    def __str__(self):
        return repr(self.message)
